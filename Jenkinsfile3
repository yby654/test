#!/usr/bin/env groovy
import hudson.model.*

node {
    stage('Docker Build image') {
        docker.withRegistry("https://registry.hub.docker.com", 'docker-id-hub') {
            sh 'docker build -t yby654/demo:latest -f ./Dockerfile .'
        }
    }
    stage('Docker Push image') {
        docker.withRegistry("https://registry.hub.docker.com", 'docker-id-hub') {
            docker.image('yby654/demo:latest').push()
        }
    }
    post {
        success {
            slack.Send (channel: '#jenkins', color: '#00FF00', message: "SUCCESSFUL: Job '${env.JOB_NAME} [${env.BUILD_NUMBER}]' (${env.BUILD_URL})")
        }
        failure {
            slack.Send (channel: '#jenkins', color: '#FF0000', message: "FAILED: Job '${env.JOB_NAME} [${env.BUILD_NUMBER}]' (${env.BUILD_URL})")
        }
    }
}